name: CI
on: [push]

jobs:
  build-macos:
    runs-on: macOS-latest
    steps:
      - uses: actions/checkout@v1
      - name: install dependencies
        run: yarn
      - name: build
        run: yarn electron-pack